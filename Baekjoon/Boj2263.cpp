#include <bits/stdc++.h>
using namespace std;

int n, in[100001], post[100001];

int main(){
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) scanf("%d", &in[i]);
    for(int i = 1; i <= n; i++) scanf("%d", &post[i]);
    
    return 0;
}